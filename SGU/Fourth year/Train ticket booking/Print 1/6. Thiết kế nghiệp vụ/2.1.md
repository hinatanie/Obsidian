1. **Define primary keys**
    - Choose `id` type for every table and make it `PRIMARY KEY`.
2. **Define foreign keys**
    - Add all relationships such as `users.role_id -> roles.id`, `flights.departure_airport_id -> airports.id`.
3. **Define `NOT NULL` constraints**
    - Decide which fields are mandatory, such as `users.email`, `flights.flight_number`, `airports.code`.
4. **Define `UNIQUE` constraints**
    - Examples: `users.email`, `roles.name`, `airports.code`, `aircrafts.registration_number`, `bookings.booking_code`, `tickets.ticket_number`.
5. **Define composite unique constraints**
    - Prevent duplicate data such as:
    - `UNIQUE(aircraft_id, seat_number)`
    - `UNIQUE(flight_schedule_id, aircraft_seat_id)`
6. **Design indexes for foreign keys**
    - Index fields frequently used in joins, such as `role_id`, `flight_id`, `aircraft_id`, `booking_id`.
7. **Design indexes for search queries**
    - Add indexes based on real queries, especially flight search by airport and departure time.
8. **Design composite indexes**
    - Example: `(departure_airport_id, arrival_airport_id)`
    - Example: `(flight_id, departure_time)`
9. **Analyze index column order**
    - Decide the order of columns in composite indexes based on `WHERE`, `JOIN`, `ORDER BY`.

10. **Design status fields**
- Define allowed states for `booking`, `flight_schedule`, `flight_seat`, `payment`, `ticket`.
14. **Define `CHECK` constraints**
- Examples: `capacity > 0`, `price >= 0`, `arrival_time > departure_time`.
15. **Define default values**
- Example: booking status = `PENDING`, seat status = `AVAILABLE`.
16. **Define delete behavior**
- Decide between `CASCADE`, `RESTRICT`, `SET NULL`, or soft delete for each FK relationship.
