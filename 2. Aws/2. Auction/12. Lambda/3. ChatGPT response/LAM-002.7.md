Do this as one small task: **edit `backend/template.yaml` so the `Resources:` section contains all 8 Lambda functions**.
Keep the header, parameters, and globals you already created. Only expand `Resources:`.
Open the file:
```
notepad template.yaml
```
Replace your current `Resources:` section with this:
```
Resources:
  AuthApiFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub auction-auth-api-${Environment}
      CodeUri: .
      Handler: lambdas.auth_api.handler.lambda_handler
  SessionApiFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub auction-session-api-${Environment}
      CodeUri: .
      Handler: lambdas.session_api.handler.lambda_handler
  ItemApiFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub auction-item-api-${Environment}
      CodeUri: .
      Handler: lambdas.item_api.handler.lambda_handler
  BidApiFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub auction-bid-api-${Environment}
      CodeUri: .
      Handler: lambdas.bid_api.handler.lambda_handler
  AdminApiFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub auction-admin-api-${Environment}
      CodeUri: .
      Handler: lambdas.admin_api.handler.lambda_handler
  RealtimeFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub auction-realtime-${Environment}
      CodeUri: .
      Handler: lambdas.realtime.handler.lambda_handler
  SessionWorkerFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub auction-session-worker-${Environment}
      CodeUri: .
      Handler: lambdas.session_worker.handler.lambda_handler
  ImageWorkerFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub auction-image-worker-${Environment}
      CodeUri: .
      Handler: lambdas.image_worker.handler.lambda_handler
```
So your full `template.yaml` should now look like this:
```
AWSTemplateFormatVersion: '2010-09-09'
Transform: AWS::Serverless-2016-10-31
Description: Auction Lambda backend
Parameters:
  Environment:
    Type: String
    AllowedValues:
      - dev
      - prod
    Default: dev
Globals:
  Function:
    Runtime: python3.11
    Timeout: 30
    MemorySize: 256
    Environment:
      Variables:
        APP_ENV: !Ref Environment
Resources:
  AuthApiFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub auction-auth-api-${Environment}
      CodeUri: .
      Handler: lambdas.auth_api.handler.lambda_handler
  SessionApiFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub auction-session-api-${Environment}
      CodeUri: .
      Handler: lambdas.session_api.handler.lambda_handler
  ItemApiFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub auction-item-api-${Environment}
      CodeUri: .
      Handler: lambdas.item_api.handler.lambda_handler
  BidApiFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub auction-bid-api-${Environment}
      CodeUri: .
      Handler: lambdas.bid_api.handler.lambda_handler
  AdminApiFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub auction-admin-api-${Environment}
      CodeUri: .
      Handler: lambdas.admin_api.handler.lambda_handler
  RealtimeFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub auction-realtime-${Environment}
      CodeUri: .
      Handler: lambdas.realtime.handler.lambda_handler
  SessionWorkerFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub auction-session-worker-${Environment}
      CodeUri: .
      Handler: lambdas.session_worker.handler.lambda_handler
  ImageWorkerFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub auction-image-worker-${Environment}
      CodeUri: .
      Handler: lambdas.image_worker.handler.lambda_handler
```
Now check the file:
```
Get-Content template.yaml
```
Then check that all 8 handler files exist:
```
Get-ChildItem .\lambdas -Recurse -Filter handler.py
```
You should see 8 files.
The important mapping is:
```
AuthApiFunction
→ lambdas/auth_api/handler.py
→ lambda_handler()
SessionApiFunction
→ lambdas/session_api/handler.py
→ lambda_handler()
ItemApiFunction
→ lambdas/item_api/handler.py
→ lambda_handler()
BidApiFunction
→ lambdas/bid_api/handler.py
→ lambda_handler()
AdminApiFunction
→ lambdas/admin_api/handler.py
→ lambda_handler()
RealtimeFunction
→ lambdas/realtime/handler.py
→ lambda_handler()
SessionWorkerFunction
→ lambdas/session_worker/handler.py
→ lambda_handler()
ImageWorkerFunction
→ lambdas/image_worker/handler.py
→ lambda_handler()
```
At this stage, **do not add API Gateway, S3 triggers, EventBridge, DynamoDB, RDS Proxy, or IAM policies yet**. LAM-002 is still just establishing the SAM resources and packaging foundation.
Your check for this small task is:
```
[ ] template.yaml has 8 AWS::Serverless::Function resources
[ ] every FunctionName uses ${Environment}
[ ] every CodeUri is .
[ ] every Handler points to the correct handler.py
[ ] no API routes are configured yet
[ ] no credentials are inside template.yaml
```
Once this is done, the next small task is **LAM-002.8 — verify dev/prod separation and make sure the template contains no secrets**.