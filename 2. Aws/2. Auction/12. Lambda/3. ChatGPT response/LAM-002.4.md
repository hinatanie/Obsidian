You can do **LAM-002.4** very simply. The goal is just to make sure every Lambda folder has a `handler.py` with a valid `lambda_handler()` function.
You are **not building the real auction logic yet**.
## Step 1 — Go to your backend folder
In PowerShell:
```
cd C:\Users\ADMIN\Documents\dhsg\projects\Live-Auction\backend
```
Check where you are:
```
pwd
```
You should be inside:
```
...\Live-Auction\backend
```
## Step 2 — Make sure the Lambda folders exist
Run:
```
mkdir lambdas\auth_api
mkdir lambdas\session_api
mkdir lambdas\item_api
mkdir lambdas\bid_api
mkdir lambdas\admin_api
mkdir lambdas\realtime
mkdir lambdas\session_worker
mkdir lambdas\image_worker
```
If PowerShell says the folders already exist, that is fine.
## Step 3 — Create `handler.py` for `auth_api`
Run:
```
notepad lambdas\auth_api\handler.py
```
Paste this:
```
def lambda_handler(event, context):
    return {
        "statusCode": 200,
        "body": "auth-api foundation ready",
    }
```
Save and close Notepad.
## Step 4 — Create `handler.py` for `session_api`
Run:
```
notepad lambdas\session_api\handler.py
```
Paste:
```
def lambda_handler(event, context):
    return {
        "statusCode": 200,
        "body": "session-api foundation ready",
    }
```
## Step 5 — Create the remaining handlers
Use the same pattern.
### `lambdas/item_api/handler.py`
```
def lambda_handler(event, context):
    return {
        "statusCode": 200,
        "body": "item-api foundation ready",
    }
```
### `lambdas/bid_api/handler.py`
```
def lambda_handler(event, context):
    return {
        "statusCode": 200,
        "body": "bid-api foundation ready",
    }
```
### `lambdas/admin_api/handler.py`
```
def lambda_handler(event, context):
    return {
        "statusCode": 200,
        "body": "admin-api foundation ready",
    }
```
### `lambdas/realtime/handler.py`
```
def lambda_handler(event, context):
    return {
        "statusCode": 200,
        "body": "realtime foundation ready",
    }
```
### `lambdas/session_worker/handler.py`
```
def lambda_handler(event, context):
    return {
        "statusCode": 200,
        "body": "session-worker foundation ready",
    }
```
### `lambdas/image_worker/handler.py`
```
def lambda_handler(event, context):
    return {
        "statusCode": 200,
        "body": "image-worker foundation ready",
    }
```
## Step 6 — Check your folder structure
Run:
```
tree lambdas /F
```
You should see something similar to:
```
lambdas
│
├── auth_api
│   └── handler.py
├── session_api
│   └── handler.py
├── item_api
│   └── handler.py
├── bid_api
│   └── handler.py
├── admin_api
│   └── handler.py
├── realtime
│   └── handler.py
├── session_worker
│   └── handler.py
└── image_worker
    └── handler.py
```
If you already created `__init__.py`, then you will see those too.
## Step 7 — Test one handler
From the `backend` folder, run:
```
python -c "from lambdas.auth_api.handler import lambda_handler; print(lambda_handler({}, None))"
```
Expected:
```
{'statusCode': 200, 'body': 'auth-api foundation ready'}
```
This proves Python can find:
```
lambdas.auth_api.handler.lambda_handler
```
## Step 8 — Test all handlers
Run these:
```
python -c "from lambdas.auth_api.handler import lambda_handler; print(lambda_handler({}, None))"
python -c "from lambdas.session_api.handler import lambda_handler; print(lambda_handler({}, None))"
python -c "from lambdas.item_api.handler import lambda_handler; print(lambda_handler({}, None))"
python -c "from lambdas.bid_api.handler import lambda_handler; print(lambda_handler({}, None))"
python -c "from lambdas.admin_api.handler import lambda_handler; print(lambda_handler({}, None))"
python -c "from lambdas.realtime.handler import lambda_handler; print(lambda_handler({}, None))"
python -c "from lambdas.session_worker.handler import lambda_handler; print(lambda_handler({}, None))"
python -c "from lambdas.image_worker.handler import lambda_handler; print(lambda_handler({}, None))"
```
All 8 should return a small dictionary without errors.
## What you are proving here
At this stage, the architecture is only:
```
AWS event
   ↓
handler.py
   ↓
lambda_handler(event, context)
   ↓
temporary response
```
Later, for example, `bid_api` becomes:
```
API Gateway
    ↓
bid_api/handler.py
    ↓
PlaceBid use case
    ↓
repository
    ↓
MySQL
```
But **do not build that yet**.
For LAM-002.4, your checklist is only:
```
[ ] 8 handler.py files exist
[ ] Every file has lambda_handler(event, context)
[ ] No FastAPI import
[ ] No Mangum import
[ ] No Uvicorn import
[ ] All 8 handlers can be imported
[ ] All 8 handlers return a temporary response
```
Once you finish this, send me the output of:
```
tree lambdas /F
```
and one example such as:
```
python -c "from lambdas.auth_api.handler import lambda_handler; print(lambda_handler({}, None))"
```
Then I’ll check it and guide you through the **next small task: LAM-002.5 — verify there are no FastAPI/Mangum/Uvicorn imports**.