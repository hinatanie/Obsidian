For **LAM-002.14**, you do not need to create a new build system. The goal is simply to prove that **one command builds all Lambda functions**.
Since your template already defines all 8 functions, the ideal command is:
```
sam build
```
But in your case, because you previously hit the `greenlet` dependency problem on Windows, use:
```
sam build --use-container
```
That still satisfies the spirit of the requirement: one repeatable command builds the complete Lambda application.
Run this from:
```
C:\Users\ADMIN\Documents\dhsg\projects\Live-Auction\backend
```
So:
```
cd C:\Users\ADMIN\Documents\dhsg\projects\Live-Auction\backend
sam build --use-container
```
Docker Desktop must be running.
If successful, you should see:
```
Build Succeeded
```
Then check:
```
Get-ChildItem .aws-sam\build
```
You should see all function build folders, such as:
```
AuthApiFunction
SessionApiFunction
ItemApiFunction
BidApiFunction
AdminApiFunction
RealtimeFunction
SessionWorkerFunction
ImageWorkerFunction
```
The important result is this:
```
template.yaml
      ↓
sam build --use-container
      ↓
all 8 Lambda functions built together
```
You should **not** need to do:
```
build auth separately
build session separately
build bid separately
copy app manually
copy dependencies manually
```
For your project, I would record the repeatable command as:
```
sam build --use-container
```
because your dependencies include compiled Python packages and you are building from Windows.
### LAM-002.14 checklist
```
[ ] Docker Desktop is running
[ ] Run only one build command
[ ] sam build --use-container succeeds
[ ] Build Succeeded appears
[ ] .aws-sam/build exists
[ ] All 8 Lambda functions appear in the build
[ ] No manual copy/build step is required
```
If `sam build --use-container` still fails, send me the full output. Then we should fix that before considering LAM-002 complete.