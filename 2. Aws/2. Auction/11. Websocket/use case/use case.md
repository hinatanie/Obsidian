When users open the same auction item page, the page must display the current number of active viewers.

When users leave page, the chat bot must show notification

When users join page, the chat bot must show notification

...
Create all use case notification on that chat bot